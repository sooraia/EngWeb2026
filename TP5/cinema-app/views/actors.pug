extends layout

block content
  h1.title ATORES
  .content
    table#table.clickable
      tr
        th Nome
        th Nº Filmes
      each f in list
        tr(onclick=`window.location='/atores/${encodeURIComponent(f.actor)}'`)
          td= f.actor 
          td= f.movies.length